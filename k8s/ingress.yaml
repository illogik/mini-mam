apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mini-mam
  namespace: default
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80,"HTTPS":443}]'
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    alb.ingress.kubernetes.io/certificate-arn: "arn:aws:acm:us-east-1:079059455431:certificate/14aedffc-7162-4a8d-b0fd-a76f8de0e7c6"
spec:
  rules:
  - host: mini-mam.iconik-work-test-acct-r4ei2.iconik.dev
    http:
      paths:
      - path: /auth
        pathType: Prefix
        backend:
          service:
            name: api-gateway
            port:
              name: http
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: api-gateway
            port:
              name: http
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              name: http
